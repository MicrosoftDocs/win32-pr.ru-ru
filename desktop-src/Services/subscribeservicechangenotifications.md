---
description: Подписывается на уведомления об изменении состояния службы с помощью функции обратного вызова.
ms.assetid: d67113eb-2141-444c-9f09-eaa772bcad8a
title: Функция Субскрибесервицечанженотификатионс (Винсвкп. h)
ms.topic: reference
ms.date: 05/31/2018
topic_type:
- APIRef
- kbSyntax
api_name:
- SubscribeServiceChangeNotifications
api_type:
- DllExport
api_location:
- SecHost.dll
- API-MS-Win-Service-Private-L1-1-0.dll
- API-MS-Win-Service-Private-L1-1-1.dll
- Advapi32.dll
- API-MS-Win-Service-Private-L1-1-2.dll
ms.openlocfilehash: e327a44d613b514123862b1ddcb1bf302fea63ef
ms.sourcegitcommit: 831e8f3db78ab820e1710cede244553c70e50500
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 01/08/2021
ms.locfileid: "105664092"
---
# <a name="subscribeservicechangenotifications-function"></a>Функция Субскрибесервицечанженотификатионс

Подписывается на уведомления об изменении состояния службы с помощью функции обратного вызова.

## <a name="syntax"></a>Синтаксис


```C++
DWORD WINAPI SubscribeServiceChangeNotifications(
  _In_     SC_HANDLE                     hService,
  _In_     SC_EVENT_TYPE                 eEventType,
  _In_     PSC_NOTIFICATION_CALLBACK     pCallback,
  _In_opt_ PVOID                         pCallbackContext,
  _Out_    PSC_NOTIFICATION_REGISTRATION *pSubscription
);
```



## <a name="parameters"></a>Параметры

<dl> <dt>

*хсервице* \[ окне\]
</dt> <dd>

Обработчик для службы или обработчик диспетчера управления службами (SCM), который отслеживает изменения.

Дескрипторы служб возвращаются функцией [**OpenService**](/windows/desktop/api/Winsvc/nf-winsvc-openservicea) и [**CreateService**](/windows/desktop/api/Winsvc/nf-winsvc-createservicea) и должны иметь права доступа к **\_ \_ состоянию запроса на обслуживание** . Дескрипторы диспетчера управления службами возвращаются функцией [**OpenSCManager**](/windows/desktop/api/Winsvc/nf-winsvc-openscmanagera) и должны иметь право на **\_ \_ Перечисление \_ доступа к службе в диспетчере SC** .

</dd> <dt>

*ивенттипе* \[ окне\]
</dt> <dd>

Указывает тип изменений состояния, о которых следует сообщать. Для этого параметра задается одно из значений, указанных [**в \_ \_ типе событий SC**](sc-event-type.md). Поведение этой функции отличается в зависимости от типа события, как показано ниже.



| Значение                                                                                                                                                                                                                                                   | Значение                                                                                                                |
|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------|
| <span id="SC_EVENT_DATABASE_CHANGE"></span><span id="sc_event_database_change"></span><dl> <dt>**SC \_ \_ \_ Изменение базы данных событий**</dt> <dt>0</dt> </dl> | Служба была добавлена или удалена. Параметр *хсервице* должен быть обработчиком SCM.<br/>                  |
| <span id="SC_EVENT_PROPERTY_CHANGE"></span><span id="sc_event_property_change"></span><dl> <dt>**SC \_ \_ \_ Изменение свойства события**</dt> <dt>1</dt> </dl> | Одно или несколько свойств службы были обновлены. Параметр *хсервице* должен быть маркером службы.<br/> |
| <span id="SC_EVENT_STATUS_CHANGE"></span><span id="sc_event_status_change"></span><dl> <dt>**SC \_ \_ \_ Изменение состояния события**</dt> <dt>2</dt> </dl>       | Состояние службы изменилось. Параметр *хсервице* должен быть маркером службы.<br/>              |



 

</dd> <dt>

*пкаллбакк* \[ окне\]
</dt> <dd>

Указывает функцию обратного вызова. Обратный вызов должен быть определен как имеющий тип **\_ \_ обратного вызова для уведомлений SC**. Дополнительные сведения см. в подразделе "Примечания".

</dd> <dt>

*пкаллбаккконтекст* \[ в необязательное\]
</dt> <dd>

Указатель, представляющий контекст для этого обратного вызова уведомления.

</dd> <dt>

*псубскриптион* \[ заполняет\]
</dt> <dd>

Возвращает указатель на подписку, полученную при регистрации обратного вызова уведомлений. Вызывающий объект отвечает за вызов [**унсубскрибесервицечанженотификатионс**](unsubscribeservicechangenotifications.md) для отмены получения уведомлений.

</dd> </dl>

## <a name="return-value"></a>Возвращаемое значение

Если функция завершается успешно, возвращается значение **ошибки \_ Success**.

Если функция завершается ошибкой, возвращаемое значение является одним из [кодов системных ошибок](/windows/desktop/Debug/system-error-codes).

## <a name="remarks"></a>Комментарии

Функция обратного вызова объявляется следующим образом:


```C++
typedef VOID CALLBACK SC_NOTIFICATION_CALLBACK(
    _In_    DWORD                   dwNotify,
    _In_    PVOID                   pCallbackContext
);
typedef SC_NOTIFICATION_CALLBACK* PSC_NOTIFICATION_CALLBACK;
```



Функция обратного вызова получает указатель на контекст, предоставленный вызывающим объектом. Обратный вызов вызывается в результате события изменения состояния службы. При вызове обратного вызова он предоставляется с битовой маской для значений **службы \_ Notify \_ * xxx***, указывающей на тип изменения состояния службы. Если обратный вызов предоставляется с нулевым значением вместо допустимого значения **службы \_ Notify \_ * xxx***, приложение должно проверить, что было изменено.

Функция обратного вызова не должна блокировать выполнение. Если предполагается, что выполнение функции ответного вызова займет некоторое время, разгрузите работу, выполняемую в функции обратного вызова, в отдельный поток, поочередно добавив рабочий элемент в поток в пуле потоков. Некоторые виды работы, которые могут сделать функцию обратного вызова, занимают некоторое время, включают выполнение файлового ввода-вывода, ожидание события и выполнение внешних вызовов удаленных процедур.

## <a name="requirements"></a>Требования



| Требование | Значение |
|-------------------------------------|----------------------------------------------------------------------------------------|
| Минимальная версия клиента<br/> | \[Только классические приложения Windows 8\]<br/>                                             |
| Минимальная версия сервера<br/> | \[Только для настольных приложений Windows Server 2012\]<br/>                                   |
| Header<br/>                   | <dl> <dt>Винсвкп. h</dt> </dl>   |
| DLL<br/>                      | <dl> <dt>SecHost.dll</dt> </dl> |



## <a name="see-also"></a>См. также раздел

<dl> <dt>

[**CreateService**](/windows/desktop/api/Winsvc/nf-winsvc-createservicea)
</dt> <dt>

[**OpenService**](/windows/desktop/api/Winsvc/nf-winsvc-openservicea)
</dt> <dt>

[**OpenSCManager**](/windows/desktop/api/Winsvc/nf-winsvc-openscmanagera)
</dt> <dt>

[**унсубскрибесервицечанженотификатионс**](unsubscribeservicechangenotifications.md)
</dt> <dt>

[**куерисервицединамиЦинформатион**](/windows/desktop/api/Winsvc/nf-winsvc-queryservicedynamicinformation)
</dt> </dl>

 

