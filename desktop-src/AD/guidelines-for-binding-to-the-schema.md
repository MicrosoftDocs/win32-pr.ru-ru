---
title: Рекомендации по привязке к схеме
description: Существует два способа привязки к схеме Active Directory привязки непосредственно к контейнеру схемы или к объекту classSchema или attributeSchema в контейнере схемы.
ms.assetid: 8c10415e-136c-476c-993c-b6dc459b5bf4
ms.tgt_platform: multiple
keywords:
- Рекомендации по привязке к схеме Active Directory
- Схема AD, привязка к
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: 65bf246a4ea1ded5c7d80c52abb8ac9192182b3b
ms.sourcegitcommit: 803f3ccd65bdefe36bd851b9c6e7280be9489016
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 08/17/2020
ms.locfileid: "103890423"
---
# <a name="guidelines-for-binding-to-the-schema"></a><span data-ttu-id="b3cbf-105">Рекомендации по привязке к схеме</span><span class="sxs-lookup"><span data-stu-id="b3cbf-105">Guidelines for Binding to the Schema</span></span>

<span data-ttu-id="b3cbf-106">Существует два способа привязки к схеме Active Directory:</span><span class="sxs-lookup"><span data-stu-id="b3cbf-106">There are two ways to bind to the Active Directory schema:</span></span>

-   <span data-ttu-id="b3cbf-107">Привязка непосредственно к контейнеру схемы или к объекту **classSchema** или **attributeSchema** в контейнере схемы.</span><span class="sxs-lookup"><span data-stu-id="b3cbf-107">Bind directly to the schema container or to a **classSchema** or **attributeSchema** object in the schema container.</span></span> <span data-ttu-id="b3cbf-108">Объекты **classSchema** или **attributeSchema** содержат полные формальные определения всех классов и атрибутов, которые могут существовать в домен Active Directory лесу.</span><span class="sxs-lookup"><span data-stu-id="b3cbf-108">The **classSchema** or **attributeSchema** objects contain complete, formal definitions of every class and attribute that can exist in an Active Directory Domain forest.</span></span> <span data-ttu-id="b3cbf-109">Дополнительные сведения см. в разделе [Reading attributeSchema and classSchema Objects](reading-attributeschema-and-classschema-objects.md).</span><span class="sxs-lookup"><span data-stu-id="b3cbf-109">For more information, see [Reading attributeSchema and classSchema Objects](reading-attributeschema-and-classschema-objects.md).</span></span>
-   <span data-ttu-id="b3cbf-110">Выполните привязку к абстрактной схеме или к записи класса или атрибута в абстрактной схеме.</span><span class="sxs-lookup"><span data-stu-id="b3cbf-110">Bind to the abstract schema or to a class or attribute entry in the abstract schema.</span></span> <span data-ttu-id="b3cbf-111">Абстрактная схема содержит только подмножество данных о каждом классе и атрибуте, но данные имеют формат, который легко получить и использовать.</span><span class="sxs-lookup"><span data-stu-id="b3cbf-111">The abstract schema contains only a subset of the data about each class and attribute, but the data is in a format that is easy to retrieve and use.</span></span> <span data-ttu-id="b3cbf-112">Дополнительные сведения см. [в разделе абстрактная схема](the-abstract-schema.md) и [Чтение абстрактной схемы](reading-the-abstract-schema.md).</span><span class="sxs-lookup"><span data-stu-id="b3cbf-112">For more information, see [The Abstract Schema](the-abstract-schema.md) and [Reading the Abstract Schema](reading-the-abstract-schema.md).</span></span>

<span data-ttu-id="b3cbf-113">Чтобы изменить или расширить схему, выполните привязку непосредственно к контейнеру схемы.</span><span class="sxs-lookup"><span data-stu-id="b3cbf-113">To modify or extend the schema, bind directly to the schema container.</span></span> <span data-ttu-id="b3cbf-114">Для чтения определений классов и атрибутов обычно бывает проще выполнить чтение из абстрактной схемы.</span><span class="sxs-lookup"><span data-stu-id="b3cbf-114">To read the class and attribute definitions, it is usually easier to read from the abstract schema.</span></span>

<span data-ttu-id="b3cbf-115">Проще читать из абстрактной схемы по следующим причинам:</span><span class="sxs-lookup"><span data-stu-id="b3cbf-115">It is easier to read from the abstract schema for the following reasons:</span></span>

-   <span data-ttu-id="b3cbf-116">ADSI предоставляет специальные методы привязки и набор интерфейсов для чтения абстрактной схемы.</span><span class="sxs-lookup"><span data-stu-id="b3cbf-116">ADSI provides special binding techniques and a set of interfaces to read the abstract schema.</span></span>
-   <span data-ttu-id="b3cbf-117">Интерфейсы ADSI, работающие с абстрактной схемой, возвращают данные в формате, подходящем для использования в других интерфейсах ADSI.</span><span class="sxs-lookup"><span data-stu-id="b3cbf-117">The ADSI interfaces that work with the abstract schema return data in a format appropriate for use in other ADSI interfaces.</span></span> <span data-ttu-id="b3cbf-118">Например, [**иадскласс**](/windows/desktop/api/iads/nn-iads-iadsclass) и [**иадспроперти**](/windows/desktop/api/iads/nn-iads-iadsproperty) обычно используют строки **lDAPDisplayName** для сообщения атрибутов и имен классов, даже несмотря на то, что эти данные хранятся в каталоге в виде идентификаторов объектов (OID).</span><span class="sxs-lookup"><span data-stu-id="b3cbf-118">For example, [**IADsClass**](/windows/desktop/api/iads/nn-iads-iadsclass) and [**IADsProperty**](/windows/desktop/api/iads/nn-iads-iadsproperty) typically use **lDAPDisplayName** strings to report attribute and class names, even though this data is stored in the directory in the form of object identifiers (OIDs).</span></span> <span data-ttu-id="b3cbf-119">Формат **lDAPDisplayName** удобен, так как другие интерфейсы ADSI используют его для ссылки на классы и атрибуты в фильтрах поиска и в других местах.</span><span class="sxs-lookup"><span data-stu-id="b3cbf-119">The **lDAPDisplayName** format is convenient because other ADSI interfaces use it to refer to classes and attributes in search filters and elsewhere.</span></span>
-   <span data-ttu-id="b3cbf-120">Запись абстрактной схемы для класса объекта содержит данные, собранные из нескольких объектов **classSchema** .</span><span class="sxs-lookup"><span data-stu-id="b3cbf-120">The abstract schema entry for an object class contains data collected from multiple **classSchema** objects.</span></span> <span data-ttu-id="b3cbf-121">Например, возможные родительские, обязательные атрибуты и необязательные атрибуты класса Object являются объединением этих атрибутов из классов класса, а также вспомогательных классов.</span><span class="sxs-lookup"><span data-stu-id="b3cbf-121">For example, the possible parents, mandatory attributes, and optional attributes for an object class are the union of these attributes from the class's superclasses and auxiliary classes.</span></span> <span data-ttu-id="b3cbf-122">При чтении данных из контейнера фактической схемы необходимо выполнить собираются данные из различных объектов **classSchema** , из которых был получен класс.</span><span class="sxs-lookup"><span data-stu-id="b3cbf-122">If you read from the actual schema container, you need to collect data from the various **classSchema** objects that the class was derived from.</span></span> <span data-ttu-id="b3cbf-123">При чтении из абстрактной схемы данные находятся в одном месте.</span><span class="sxs-lookup"><span data-stu-id="b3cbf-123">If you read from the abstract schema, the data is in one place.</span></span>

<span data-ttu-id="b3cbf-124">Необходимо выполнить привязку непосредственно к контейнеру схемы, а не использовать абстрактную схему в следующих случаях.</span><span class="sxs-lookup"><span data-stu-id="b3cbf-124">You should bind directly to the schema container rather than using the abstract schema in the following cases:</span></span>

-   <span data-ttu-id="b3cbf-125">Для получения конкретных атрибутов, не представленных в абстрактной схеме.</span><span class="sxs-lookup"><span data-stu-id="b3cbf-125">To get specific attributes not exposed in the abstract schema.</span></span> <span data-ttu-id="b3cbf-126">Например, **oMSyntax**, **attributeSchema**, **дефаултсекуритидескриптор** и другие атрибуты не представлены в абстрактной схеме.</span><span class="sxs-lookup"><span data-stu-id="b3cbf-126">For example, **oMSyntax**, **attributeSchema**, **defaultSecurityDescriptor**, and other attributes are not exposed in the abstract schema.</span></span>
-   <span data-ttu-id="b3cbf-127">Для запроса объектов **attributeSchema** и **classSchema** .</span><span class="sxs-lookup"><span data-stu-id="b3cbf-127">To query for **attributeSchema** and **classSchema** objects.</span></span> <span data-ttu-id="b3cbf-128">Для поиска классов или атрибутов, соответствующих заданному фильтру, необходимо выполнить привязку к контейнеру схемы и выполнить одноуровневый поиск.</span><span class="sxs-lookup"><span data-stu-id="b3cbf-128">To search for classes or attributes that match a specified filter, bind to the schema container and perform a one-level search.</span></span>
-   <span data-ttu-id="b3cbf-129">Для добавления или изменения атрибутов или классов.</span><span class="sxs-lookup"><span data-stu-id="b3cbf-129">To add or modify attributes or classes.</span></span> <span data-ttu-id="b3cbf-130">Абстрактная схема доступна только для чтения. его нельзя использовать для изменения или расширения схемы.</span><span class="sxs-lookup"><span data-stu-id="b3cbf-130">The abstract schema is read-only; you cannot use it to modify or extend the schema.</span></span> <span data-ttu-id="b3cbf-131">Имейте в виду, что изменения должны выполняться на контроллере домена, который является хозяином схемы.</span><span class="sxs-lookup"><span data-stu-id="b3cbf-131">Be aware that modifications must be made at the domain controller that is the schema master.</span></span> <span data-ttu-id="b3cbf-132">Дополнительные сведения см. в разделе [Предварительные требования для установки расширения схемы](prerequisites-for-installing-a-schema-extension.md).</span><span class="sxs-lookup"><span data-stu-id="b3cbf-132">For more information, see [Prerequisites for Installing a Schema Extension](prerequisites-for-installing-a-schema-extension.md).</span></span>

 

 