---
title: Общие атрибуты ресурсов
description: инструкции определения ресурсов, поддерживаемые 16-разрядным Windows, включают параметр load-mem, указывающий характеристики загрузки и памяти ресурса.
ms.assetid: 53740997-854b-447c-9ab1-de8e17c0de1e
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: d8b7bf01f95c700f12d130490673ef4f0df61cb84ae8077ca759655a2c4a1577
ms.sourcegitcommit: e858bbe701567d4583c50a11326e42d7ea51804b
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 08/11/2021
ms.locfileid: "118235581"
---
# <a name="common-resource-attributes"></a>Общие атрибуты ресурсов

инструкции определения ресурсов, поддерживаемые 16-разрядным Windows, включают параметр *load-mem* , указывающий характеристики загрузки и памяти ресурса. Эти атрибуты разрешены в скриптах ресурсов для обратной совместимости, но они не учитываются. ресурсы Windows загружаются при загрузке соответствующего модуля и освобождаются при выгрузке модуля.

## <a name="load-attributes"></a>Загрузить атрибуты

Атрибуты нагрузки указывают время загрузки ресурса. Параметр Load должен быть одним из следующих атрибутов.



| attribute      | Описание                                                                  |
|----------------|------------------------------------------------------------------------------|
| **Установка**    | Не обрабатывается. в 16-разрядной Windows ресурс загружается с исполняемым файлом. |
| **лоадонкалл** | Не обрабатывается. в 16-разрядной Windows ресурс загружается при вызове.              |



 

## <a name="memory-attributes"></a>Атрибуты памяти

Атрибуты памяти указывают, является ли ресурс фиксированным или перемещаемым, является ли он недоступным и является ли он чистым. Параметр Memory может быть одним или несколькими следующими атрибутами.



| attribute       | Описание                                                                                                                               |
|-----------------|-------------------------------------------------------------------------------------------------------------------------------------------|
| **FIXED**       | Не обрабатывается. в 16-разрядных Windows ресурс остается в фиксированном расположении в памяти.                                                              |
| **MOVEABLE**    | Не обрабатывается. в 16-разрядных Windows ресурс можно переместить, если это необходимо для сжатия памяти.                                                     |
| **ОТКЛОНЕНо** | Не обрабатывается. в 16-разрядных Windows ресурс может быть отклонен, если он больше не нужен.                                                            |
| **ЧИСТЫМИ**        | Не обрабатывается. Принимается на совместимость с существующими скриптами ресурсов.                                                                       |
| **НЕЧИСТЫМИ**      | Не обрабатывается. Принимается на совместимость с существующими скриптами ресурсов.                                                                       |
| **ИСПОЛЬЗУЕМЫЙ**      | Не обрабатывается. в 16-разрядных Windows для обычных модулей параметр shared не учитывается. для ресурса из модуля пзу Windows память является общей.        |
| **НЕ ЯВЛЯЮЩАЯСЯ общей**   | Не обрабатывается. в 16-разрядных Windows для обычных модулей игнорируется несовместное использование. для ресурса из модуля пзу Windows память не используется совместно. |



 

 

 




